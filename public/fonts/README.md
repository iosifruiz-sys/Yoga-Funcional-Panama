# Molot font

Place the licensed webfont here as `Molot.woff2`. The global stylesheet already
registers it. Until then, the declared local fallback uses heavy system display
faces without stretching or artificially condensing them.
